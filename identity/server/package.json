{
  "name": "stripe-sample-demo",
  "version": "1.0.0",
  "description": "A Stripe demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "browserify --node --ignore-missing server.js | terser > bundle2.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stripe-demos",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-cdn": "^0.4.0",
    "path": "^0.12.7",
    "stripe": "^10.1.0",
    "uniq": "^1.0.1"
  }
}
